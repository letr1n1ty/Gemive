{
  "name": "gemive",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension for live translated subtitles and interpretation using Gemini Live Translate.",
  "scripts": {
    "check": "node scripts/check-syntax.mjs && npm run typecheck",
    "typecheck": "tsc --noEmit",
    "zip": "node scripts/package-zip.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@types/chrome": "^0.0.326"
  }
}